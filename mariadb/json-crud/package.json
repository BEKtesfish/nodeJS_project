{
  "name": "hello",
  "version": "1.0.0",
  "description": "Exercise: Setup an express server for \"Hello World\"",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env app",
    "dev": "npx nodemon --env-file=.env app",
    "test": "NODE_ENV=test mocha test/**/*.test.js",
    "clean": "rm -rf node_modules/ package-log.json",
    "eslint": "npx eslint .",
    "eslint:fix": "npx eslint . --fix",
    "jsdoc": "npx jsdoc -c jsdoc.config.json",
    "coverage": "npx nyc npm test"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.0.1",
    "express-ejs-layouts": "^2.5.1",
    "express-session": "^1.18.1",
    "morgan": "^1.10.0",
    "mysql2": "^3.12.0"
  },
  "devDependencies": {
    "eslint-plugin-jsdoc": "^50.6.3",
    "nodemon": "^3.1.9"
  }
}
