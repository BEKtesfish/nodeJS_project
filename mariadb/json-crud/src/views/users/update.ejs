
<%- include('header') -%>
<form id="updateForm"style="display:flex; flex-direction:column; width:500px; margin:auto; padding:20px; gap:10px;" method="POST">
    <label for="userId">Name:</label>
    <input type="text" id="userId" name="userId" value= "<%= user.id%>" readonly>
    <label for="name">Name:</label>
    <input type="text" id="name" name="name" value = "<%= user.username%>" readonly>
    <label for="email">Email:</label>
    <input type="email" id="email" name="email" value="<%= user.email%>" required>
    <button type="submit">Add User</button>
</form>

<!-- <script>
    document.getElementById("updateForm").addEventListener("submit", (e)=>{
        e.preventDefault();
        let userId = document.getElementById("userId").value;
        let email = document.getElementById("email").value;
        fetch(`http://localhost:3001/crud/users/${userId}/update`,{
            method:"put",
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({email})

        }).then(response => response.json())
        .then(data => {
            console.log(data);
         
          
        });
        
    });
</script> -->